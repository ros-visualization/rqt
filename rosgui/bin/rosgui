#!/usr/bin/env python

try:
    import os, sys
    import rospkg
    r = rospkg.RosPack()
    p = r.get_path('rosgui')
    sys.path.append(os.path.join(p, 'src'))
except ImportError:
    import roslib
    roslib.load_manifest('rosgui')

from rosgui import rosgui_main
rosgui_main.rosgui_main()
