/**
\mainpage
\htmlinclude manifest.html

\b rqt_gui provides the main to start an instance of the integrated graphical user interface provided by qt_gui.
The code API of rqt_gui (except the exposed interfaces for plugins) is not stable, so it is not recommended for use in other tools.
The code API of qt_gui_cpp (except the exposed interfaces for plugins, namely rqt_gui_cpp::Plugin) is not stable, so it is not recommended for use in other tools.
*/
